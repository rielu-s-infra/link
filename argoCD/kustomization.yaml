apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: rielu-links2
resources:
  - deployment.yaml
  - service.yaml
  - ingress.yaml
  - imageUpdater.yaml
  - hpa.yaml
  - service-uptimekuma.yaml
labels:
  - includeSelectors: true
    pairs:
      app: rielu-links2
